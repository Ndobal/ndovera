# NDOVERA Frontend App Shell

This directory is the target frontend shell for the unified NDOVERA architecture.

- app: routing and shell bootstrap
- layout: app-wide layouts
- components: reusable UI composition
- modules: domain-owned frontend features
