<div key={label} className="flex justify-between border-b border-gray-200">
            <span className="font-bold">{label}:</span>
            <span className="uppercase">{val}</span>
          </div>
        ))}
      </div>

      {/* Skills Matrix */}
      <div className="flex-grow overflow-hidden">
        <table className="w-full border-collapse border border-black text-[10px]">
          <thead>
            <tr className="bg-gray-200">
              <th className="border border-black p-1 text-left">SKILL ACCESSED</th>
              {gradingScales.map(g => <th key={g} className="border border-black p-1 w-16 text-[8px]">{g}</th>)}
            </tr>
          </thead>
          <tbody>
            {data.skills.map((sec, idx) => (
              <React.Fragment key={idx}>
                <tr className="bg-gray-50 font-bold border-t border-black">
                  <td colSpan="6" className="p-1 border border-black">{idx + 1}. {sec.category}</td>
                </tr>
                {sec.items.map((item, iIdx) => (
                  <tr key={iIdx}>
                    <td className="p-1 border border-black pl-4">{item.label}</td>
                    {gradingScales.map(g => (
                      <td key={g} className="border border-black text-center font-bold text-xs">
                        {item.rating === g ? "✓" : ""}
                      </td>
                    ))}
                  </tr>
                ))}
              </React.Fragment>
            ))}
          </tbody>
        </table>
      </div>

      {/* Physical Development & Comments */}
      <div className="mt-4 space-y-4">
        <div className="border border-black">
          <p className="bg-gray-100 text-[10px] font-bold p-1 text-center border-b border-black">PHYSICAL DEVELOPMENT</p>
          <div className="grid grid-cols-4 text-[10px] text-center italic">
            <div className="border-r border-black">Height (Start): {data.heightStart || '-'}</div>
            <div className="border-r border-black">Height (End): {data.heightEnd || '-'}</div>
            <div className="border-r border-black">Weight (Start): {data.weightStart || '-'}</div>
            <div>Weight (End): {data.weightEnd || '-'}</div>
          </div>
        </div>

        <div className="text-xs">
          <p className="font-bold italic underline">Teacher's Comment:</p>
          <p className="p-2 border-b border-black leading-tight italic">{data.teacherComment}</p>
          <p className="font-bold italic underline mt-2">Head of School's Remark:</p>
          <p className="p-2 border-b border-black leading-tight italic">"{data.headComment}"</p>
        </div>
      </div>

      {/* Signatures */}
      <div className="mt-auto grid grid-cols-2 gap-10 pt-6 text-[10px] font-bold uppercase text-center">
        <div className="border-t border-black pt-1">Parent's Signature & Date</div>
        <div className="border-t border-black pt-1">School Stamp & Signature</div>
      </div>
    </div>
  );
};

export default EarlyExplorersResult;
2. Progress Report (Template for All Nursery Classes)
Used for the broader Nursery section, focusing on developmental milestones with "Not Yet/Progressing/Yes" grading.

JavaScript
import React from 'react';

const NurseryProgressReport = ({ student }) => {
  return (
    <div className="a4-page font-sans text-gray-800 border border-gray-300">
      <div className="text-center mb-6">
        <h1 className="text-4xl font-black text-blue-900 uppercase">Genesis International School</h1>
        <div className="mt-2 inline-block border-2 border-blue-900 px-8 py-1 rounded-full bg-blue-900 text-white font-bold tracking-tighter text-xl">
          NURSERY PROGRESS REPORT
        </div>
      </div>

      <div className="grid grid-cols-2 gap-4 mb-6 border-b-2 border-black pb-4 uppercase text-xs font-bold">
        <div className="flex border-b border-gray-300">Name: <span className="ml-2 text-blue-800">{student.name}</span></div>
        <div className="flex border-b border-gray-300">Term: <span className="ml-2">{student.term}</span></div>
        <div className="flex border-b border-gray-300">Year: <span className="ml-2">{student.year}</span></div>
        <div className="flex border-b border-gray-300">Class: <span className="ml-2">Nursery</span></div>
      </div>

      <div className="flex-grow">
        <table className="w-full border-collapse border-2 border-black text-[10px]">
          <thead className="bg-gray-100 uppercase">
            <tr className="border-b-2 border-black">
              <th className="p-2 border-r border-black w-8">#</th>
              <th className="p-2 border-r border-black text-left">Developmental Skills</th>
              <th className="p-2 border-r border-black bg-red-50 w-20">Not Yet</th>
              <th className="p-2 border-r border-black bg-yellow-50 w-20">Progressing</th>
              <th className="p-2 bg-green-50 w-20">Yes</th>
            </tr>
          </thead>
          <tbody>
            {student.sections.map((section) => (
              <React.Fragment key={section.id}>
                <tr className="bg-gray-200 border-b border-black font-extrabold uppercase">
                  <td className="p-1 border-r border-black text-center">{section.id}</td>
                  <td colSpan="4" className="p-1">{section.title}</td>
                </tr>
                {section.items.map((item, idx) => (
                  <tr key={idx} className="border-b border-gray-300">
                    <td className="p-1 border-r border-black text-center text-gray-400">{item.no}</td>
                    <td className="p-1 border-r border-black">{item.text}</td>
                    <td className="border-r border-black text-center font-bold text-red-600 text-sm">{item.status === 'not_yet' ? '✓' : ''}</td>
                    <td className="border-r border-black text-center font-bold text-yellow-600 text-sm">{item.status === 'progressing' ? '✓' : ''}</td>
                    <td className="text-center font-bold text-green-600 text-sm">{item.status === 'yes' ? '✓' : ''}</td>
                  </tr>
                ))}
              </React.Fragment>
            ))}
          </tbody>
        </table>
      </div>

      <div className="mt-8 grid grid-cols-2 gap-12 text-xs text-center font-bold uppercase">
        <div className="border-t-2 border-black pt-2 italic">Class Teacher's Signature</div>
        <div className="border-t-2 border-black pt-2 italic">Head of School's Signature</div>
      </div>
    </div>
  );
};

export default NurseryProgressReport;
3. Cognitive Report (Template for All Primary Classes - Page 2)
This acts as the second page for Primary reports, specifically focusing on Cognitive and Creative scores using the 1-5 scale.

JavaScript
import React from 'react';

const PrimaryCognitivePage2 = ({ studentData }) => {
  const ratingLegend = [
    { level: 5, label: "EXCELLENT" }, { level: 4, label: "GOOD" },
    { level: 3, label: "ACCEPTABLE LEVEL" }, { level: 2, label: "BELOW ACCEPTABLE LEVEL" },
    { level: 1, label: "POOR" }
  ];

  return (
    <div className="a4-page font-serif text-gray-900 border border-gray-400">
      <div className="flex justify-between items-start mb-6 border-b-2 border-black pb-4">
        <div className="w-2/3 pt-4">
          <h1 className="text-2xl font-bold uppercase text-blue-900">Genesis International School</h1>
          <p className="text-lg font-extrabold italic underline mt-2">PAGE 2: COGNITIVE & DEVELOPMENTAL REPORT</p>
          <div className="mt-4 flex items-center border-b border-black w-full uppercase">
            <span className="font-bold mr-2 text-sm">Student Name:</span>
            <span className="text-base">{studentData.name}</span>
          </div>
        </div>

        {/* Legend Box */}
        <div className="w-48 border border-black p-1 bg-gray-50 text-[9px]">
          <p className="font-bold text-center border-b border-black mb-1">PERFOMANCE LEVEL RATING</p>
          {ratingLegend.map(r => (
            <div key={r.level} className="flex"><span className="w-4 font-bold">{r.level}</span><span>{r.label}</span></div>
          ))}
        </div>
      </div>

      <div className="flex-grow space-y-4">
        {/* Sections A, B, and C */}
        {[
          { id: "A", title: "(COGNITIVE)", content: [
              { sub: "LANGUAGE SKILLS", items: studentData.scores.languageSkills },
              { sub: "MATHEMATICS SKILLS", items: studentData.scores.mathSkills }
            ]},
          { id: "B", title: "PERSONAL SOCIAL AND EMOTIONAL DEVELOPMENT", items: studentData.scores.personalSocial },
          { id: "C", title: "CREATIVE DEVELOPMENT", items: studentData.scores.creativeDev }
        ].map((sec) => (
          <div key={sec.id} className="border border-black overflow-hidden">
            <div className="bg-gray-800 text-white p-1 text-center font-bold text-xs">{sec.id}. {sec.title}</div>
            <table className="w-full text-[10px]">
              <tbody>
                {sec.content ? sec.content.map(sub => (
                  <React.Fragment key={sub.sub}>
                    <tr className="bg-gray-200 font-bold border-y border-black"><td colSpan="2" className="px-2">{sub.sub}</td></tr>
                    {sub.items.map((item, idx) => (
                      <tr key={idx} className="border-b border-gray-200"><td className="p-1 pl-4 uppercase">{item.label}</td><td className="w-12 border-l border-black text-center font-bold">{item.rating || "-"}</td></tr>
                    ))}
                  </React.Fragment>
                )) : sec.items.map((item, idx) => (
                  <tr key={idx} className="border-b border-gray-200"><td className="p-1 pl-4 uppercase">{item.label}</td><td className="w-12 border-l border-black text-center font-bold">{item.rating || "-"}</td></tr>
                ))}
              </tbody>
            </table>
          </div>
        ))}
      </div>

      <div className="mt-6 border-2 border-black p-2 min-h-[80px]">
        <p className="text-[10px] font-bold uppercase underline">Head of School's Remark & Stamp:</p>
        <p className="text-xs italic pt-2">{studentData.remarks || "An impressive outing. Keep up the hard work."}</p>
      </div>

      <div className="mt-4 flex justify-end text-[10px] font-bold">
        <div className="w-40 border-t border-black text-center pt-1 uppercase">Authorised Signature</div>
      </div>
    </div>
  );
};

export default PrimaryCognitivePage2;
Key Template Implementation Notes:
Print Constraints: Each component is wrapped in the .a4-page class. The flex-grow property on tables ensures they fill the vertical space appropriately without overflowing the 297mm height.

British English: All labels (e.g., "Honours", "Organised", "Programme", "Colours", "Authorised") use British spelling.

Data Flow:

Explorers Result: High-detail skill ticks + Physical growth.

Progress Report: Standardised Nursery tracking (Not Yet to Yes).

Cognitive Report: Numerical logic (1 to 5) for Primary levels.

Exact Match: I have preserved the slight typos (like "PERFOMANCE") from the original Excel files to ensure they remain 100% familiar to your current school administration, though you can correct them in the code if preferred.