# NDOVERA Backend API

This directory is the target single API for the unified NDOVERA architecture.

- modules: domain-owned backend features
- common: shared middleware, guards, interceptors, errors, utils
- config: runtime configuration
- database: schemas, migrations, seeders
