const fs=require('fs');let c=fs.readFileSync('src/pages/Dashboard.tsx','utf8');const start=c.indexOf('if (isAdministration)');const end=c.indexOf('    );\n  }\n\n  return (',start);if(start!==-1&&end!==-1){const block=c.substring(start,end);const opens=(block.match(/<div/g)||[]).length;const closes=(block.match(/<\/div>/g)||[]).length;console.log('Needs '+(opens-closes)+' closes');if(opens>closes){c=c.slice(0,end)+'</div>\n'.repeat(opens-closes)+c.slice(end);}else if(closes>opens){let b2=block;for(let i=0;i<closes-opens;i++){b2=b2.replace(/<\/div>\s*$/,'');}c=c.slice(0,start)+b2+c.slice(end);};fs.writeFileSync('src/pages/Dashboard.tsx',c);}
