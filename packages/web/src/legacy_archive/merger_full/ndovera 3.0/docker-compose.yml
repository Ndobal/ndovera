version: '3.8'
services:
  frontend:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped
    # Uncomment if you want to serve custom nginx config
    # volumes:
    #   - ./nginx.conf:/etc/nginx/nginx.conf
